import RunnableExample from "../runnable_example";
import Card from "../card";

### Строки

Строки — это фрагменты текста, заключённые в кавычки (`"` и `"`). Вы уже
видели строки в предыдущих примерах, но вот ещё несколько:

<Card type="try_it">Попробуйте изменить некоторые строки ниже.</Card>

<RunnableExample
  code={`say("Привет, мир!");
say("Я строка!");
say("Я тоже строка!");`}
/>
